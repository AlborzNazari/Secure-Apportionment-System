git add __wrangler.toml
git commit -m "fix: add wrangler.toml for static asset deployment"
git push
```

Then in Cloudflare when you create the application — **only fill in the deploy command as:**
```
wrangler deploy
